import { View, Text, SafeAreaView } from "react-native";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <SafeAreaView>
      <Text>Page for services</Text>
    </SafeAreaView>
  );
};

export default Page;
